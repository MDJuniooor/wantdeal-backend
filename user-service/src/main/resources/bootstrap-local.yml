# user-service bootstrap-local.yml
spring:
  profiles: local
  cloud:
    config:
#      uri: http://localhost:8888
      fail-fast: true
      discovery:
        service-id: configserver
        enabled: true
