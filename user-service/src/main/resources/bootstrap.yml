# member-service bootstrap.yml
spring:
  profiles:
    active: local
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: info, refresh

